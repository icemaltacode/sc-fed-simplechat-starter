import styled from "styled-components";

const FlexContainer = styled.div`
  display: flex;
  flex-flow: column;
`;

function App() {
  return (
    <FlexContainer>
      <h1>Simplechat</h1>
      {/* TODO: Add the other components */}
    </FlexContainer>
  );
}

export default App;
